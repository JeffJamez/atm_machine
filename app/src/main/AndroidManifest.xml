<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".views.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".views.LoginActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".views.DashboardActivity"
            android:exported="false"
            android:label="Dashboard" />

        <activity
            android:name=".views.WithdrawActivity"
            android:exported="false"
            android:label="Withdraw Cash"
            android:parentActivityName=".views.DashboardActivity" />

        <activity
            android:name=".views.DepositActivity"
            android:exported="false"
            android:label="Deposit Cash"
            android:parentActivityName=".views.DashboardActivity" />

        <activity
            android:name=".views.TransferActivity"
            android:exported="false"
            android:label="Transfer Funds"
            android:parentActivityName=".views.DashboardActivity" />

        <activity
            android:name=".views.BalanceActivity"
            android:exported="false"
            android:label="Account Balance"
            android:parentActivityName=".views.DashboardActivity" />

        <activity
            android:name=".views.TransactionHistoryActivity"
            android:exported="false"
            android:label="Transaction History"
            android:parentActivityName=".views.DashboardActivity" />

    </application>

</manifest>